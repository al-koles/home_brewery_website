import 'package:flutter/material.dart';

class Constants {
  static String baseUrl = 'https://localhost:44372/api';

  static const boxDecoration = BoxDecoration(
    borderRadius: BorderRadius.all(
      Radius.circular(20),
    ),
    color: Color(0xFFFFFACE),
  );
}